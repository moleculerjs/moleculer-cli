{
  "name": "{{projectName}}",
  "version": "0.1.0",
  "description": "{{projectDescription}}",
  "main": "index.js",
  "scripts": {
    "demo": "node examples/index.js",
    {%- if lint %}"lint": "eslint --ext=.js src test",{% endif %}
    "deps": "npm-check -u"
  },
  "author": "{{fullName}}",
  "license": "MIT",
  "devDependencies": {
    {%- if lint -%}
    "eslint": "3.19.0",
    {%- endif %}
    "jest": "20.0.0",
    "jest-cli": "20.0.0"
  }
}
